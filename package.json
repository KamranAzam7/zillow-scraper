{
  "name": "zillow-scraper",
  "version": "1.0.0",
  "description": "A Nodejs script that scrapes data from Zillow profiles.",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^18.7.23",
    "nodemon": "^2.0.20",
    "puppeteer": "^18.0.5",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "csv-writer": "^1.6.0",
    "fs-extra": "^11.2.0",
    "puppeteer-extra": "^3.3.4",
    "puppeteer-extra-plugin-stealth": "^2.11.1"
  }
}
